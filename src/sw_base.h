#ifndef SW_BASE
#define SW_BASE

enum SW_Split{
    Single = 0,
    Split_Horizontal = 1,
    Split_Vertical = 2
};

enum SW_ActiveWidget{
    CodeEditor = 0,
    SceneView = 1,
    Timeline = 2,
    Outliner = 3
};

#endif // SW_BASE

